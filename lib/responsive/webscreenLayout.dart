import 'package:flutter/material.dart';

class WebScrennLayout extends StatelessWidget {
  const WebScrennLayout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('web'),
    );
  }
}